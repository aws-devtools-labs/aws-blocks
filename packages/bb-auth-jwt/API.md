## API Report File for "@aws-blocks/bb-auth-jwt"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { AuthUser } from '@aws-blocks/auth-common';
import { BlocksAuth } from '@aws-blocks/auth-common';
import { BlocksContext } from '@aws-blocks/core';
import type { ChildLogger } from '@aws-blocks/bb-logger';
import { JWTPayload } from 'jose';
import { JWTVerifyGetKey } from 'jose';
import { Scope } from '@aws-blocks/core';
import { ScopeParent } from '@aws-blocks/core';

// @public
export class AuthBearerJwt extends Scope implements BlocksAuth {
    constructor(scope: ScopeParent, id: string, options: AuthBearerJwtOptions);
    // (undocumented)
    checkAuth(context: BlocksContext): Promise<boolean>;
    // (undocumented)
    getCurrentUser(context: BlocksContext): Promise<AuthBearerUser | null>;
    // @internal (undocumented)
    protected log: ChildLogger;
    // (undocumented)
    requireAuth(context: BlocksContext): Promise<AuthBearerUser>;
}

// @public
export const AuthBearerJwtErrors: {
    readonly MissingToken: "MissingTokenException";
    readonly InvalidToken: "InvalidTokenException";
    readonly UnsupportedAlgorithm: "UnsupportedAlgorithmException";
    readonly MissingClaim: "MissingClaimException";
    readonly JwksFetchFailed: "JwksFetchFailedException";
};

// @public
export interface AuthBearerJwtOptions {
    audience?: string;
    clockTolerance?: number;
    hmacSecret?: SecretLike;
    issuer: string | string[];
    jwks?: string | Record<string, string> | JWTVerifyGetKey;
    jwksCacheMaxAge?: number;
    logger?: ChildLogger;
    mapUser?: (claims: JWTPayload & Record<string, unknown>) => AuthBearerUser;
    requiredClaims?: string[];
    subjectClaim?: string;
}

// @public
export interface AuthBearerUser extends AuthUser {
    // (undocumented)
    claims: JWTPayload & Record<string, unknown>;
}

export { AuthUser }

export { BlocksAuth }

// @public
export type SecretLike = string | (() => string | Promise<string>);

// (No @packageDocumentation comment for this package)

```
