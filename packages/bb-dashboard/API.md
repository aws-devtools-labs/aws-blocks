## API Report File for "@aws-blocks/bb-dashboard"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { Compute } from '@aws-blocks/core/cdk/internal';
import type { ScopeParent } from '@aws-blocks/core';

// @public
export class Dashboard {
    constructor(scope: ScopeParent, id: string, options?: DashboardOptions);
    readonly dashboardName: string;
    readonly fullId: string;
    readonly url: string | null;
}

// @public
export const DashboardErrors: {
    readonly InvalidMetricConfig: "InvalidMetricConfigException";
};

// @public
export interface DashboardOptions {
    computes?: Compute[];
    dashboardName?: string;
    defaultTimeRange?: string;
    logs?: boolean;
    metrics?: MetricsSource | MetricsSource[];
    routePath?: string | false;
    title?: string;
    traces?: boolean;
}

// @public
export interface MetricConfig {
    dimensions?: Record<string, string>;
    name: string;
    period?: number;
    stat?: 'Sum' | 'Average' | 'Maximum' | 'Minimum' | 'p99' | 'p95' | 'p50';
    title?: string;
}

// @public
export interface MetricsBBRef {
    readonly defaultDimensions?: Record<string, string>;
    // (undocumented)
    readonly namespace: string;
}

// @public
export interface MetricsSource {
    metricConfigs?: MetricConfig[];
    metrics: MetricsBBRef;
}

// (No @packageDocumentation comment for this package)

```
