---
---

Add an isolated, comprehensive E2E telemetry test suite (`test-apps/telemetry`) that verifies `--telemetry-file` emission and attributes, identifier creation, per-command success/failure events, and the pinned installation-id recreation lifecycle. Test-only change — no production source changes.
