observability_correctness — Does api.ping (a method on an ApiNamespace named `api`, returning EXACTLY `{ status: "ok" }` with no extra fields) genuinely instrument EVERY invocation with all four blocks: (a) write a real log line via the Logger block, (b) emit a named metric/counter via the Metrics block, and (c) run its actual work INSIDE a `Tracer.startSegment(...)` wrapper (not merely create an unused segment alongside the work)? Is the displayed app name read at runtime from an AppSetting block via `.get()` — initial value exactly `"Observability Service"` — and never hard-coded in the page HTML? Is the JSON-RPC response spec-compliant (echoes the request `id`, includes `jsonrpc: "2.0"`, carries `Content-Type: application/json`, and an unknown method returns a JSON-RPC error envelope rather than a 5xx or a bogus success)? Are the two additional `api` methods implemented to contract — `api.info` returning EXACTLY `{ name, uptimeMs }` (name read from the AppSetting via `.get()`, `uptimeMs` a non-negative number, no extra fields) and `api.echo` returning EXACTLY `{ echo }` with the argument's original type preserved (a number is not stringified) and a missing/empty argument rejected with a JSON-RPC error envelope rather than a degenerate `{ echo: null }` — and is each of info/echo instrumented (its own `startSegment` wrapper plus a log line) like ping? A malformed array/batch body must yield an `error.code === -32600` envelope, never a 5xx. Score DOWN if these methods are missing, leak extra fields, stringify the echoed value, skip input validation, or run their work outside a tracer segment. Is the `/status` page served as `text/html` via a RawRoute, do unmatched paths 404 (no catch-all), and is `ping` stateless / safe under concurrent calls? Score DOWN whenever any of the four blocks is missing, stubbed, or cosmetic — e.g. a logger/metrics import that is never called, a tracer segment created but with the work running outside it, or the app name hard-coded — even if the endpoint still returns ok.
