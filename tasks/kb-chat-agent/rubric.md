agent_rag_tool_correctness — Does a real agent block (Bedrock Claude Sonnet 4.6) answer chat questions by genuinely retrieving from a knowledge-base block that indexes a `knowledge/` folder the builder seeded (the reply repeats the unique seeded facts, not hard-coded text) and by calling a named tool whose deterministic output appears in the reply, with the UI attributing tool use and citing the source document — rather than canned answers, inline string matching, or a fake retrieval?
