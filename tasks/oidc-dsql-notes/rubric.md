auth_correctness — Does the implementation gate access behind an OIDC sign-in (AuthOIDC + `stubIdp` named exactly `stub`, established via the server-initiated signin route `/aws-blocks/auth/signin/stub` so the session cookie is set server-side and restored on load via `onAuthChange`) and expose the signed-in subject id — and, the main focus, persist per-user notes in a DistributedDatabase table with real data depth: a `.sql` migration, user-scoped **parameterized** INSERT/SELECT, note text stored and rendered **verbatim** (single quotes like `' OR '1'='1`, unicode/emoji, and literal HTML like `<b>x</b>` all round-trip intact as text content and are never executed), a stable explicit `ORDER BY` (oldest-first, identical on every load and after adding more), **no deduplication** (the same text twice yields two rows), and all of a user's notes plus a still-functional editor surviving a full page reload?
